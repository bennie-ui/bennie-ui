import { Unstyled } from "@storybook/blocks";
import { Section } from '../../../../components/primitives/section'
import { Button } from '../../../../components/primitives/button'
import { variants } from '../../../../libraries/baseline/tailwind/attributes/actions'

## Example
```
<Button action='primary'>content</Button>
```
## Types
[See Button values](https://github.com/bolanosdev/phoenix-ui/blob/main/libraries/types/sizing/opacity.types.ts)

# Compiled examples 

{
  variants.base.map((action, idx) => {
    return <Unstyled key={idx}>
      <Button action={action}>
        {action}
      </Button>
    </Unstyled>
  })
}

