import { Unstyled } from "@storybook/blocks";
import { Section } from '@phoenix-ui/section'
import { Text } from '@phoenix-ui/text'
import { variants } from '@phoenix-ui/baseline/tailwind/colors/background/sm'

# Demo of supported text-orange-* values

## Example
```
<Text colors={{ background: { color: 'orange', weight: 400 } }}>Content</Text>
```
## Types
[See Margin Values](https://github.com/bolanosdev/phoenix-ui/blob/main/libraries/types/sizing/width.types.ts)


# Orange tests
{
  variants.base.orange.map((value, idx) => {
    const [_, color, weight] =  value.split('-')
    return <Unstyled key={idx}>
      <Section className="w-full" colors={{ background: {color, weight}, text: { color: 'slate' } }}>
          {value}
      </Section>
    </Unstyled>
  })
}



