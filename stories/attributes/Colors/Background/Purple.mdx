import { Unstyled } from "@storybook/blocks";
import { Section } from '../../../../components/primitives/section'
import { Text } from '../../../../components/primitives/text'
import { variants } from '../../../../libraries/baseline/tailwind/colors/background/sm'

# Demo of supported text-purple-* values

## Example
```
<Text colors={{ text: { color: 'purple', weight: 400 } }}>Content</Text>
```
## Types
[See Margin Values](https://github.com/bolanosdev/phoenix-ui/blob/main/libraries/types/sizing/width.types.ts)


# Neutral tests
{
  variants.base.purple.map((value, idx) => {
    const [_, color, weight] =  value.split('-')

    return <Unstyled key={idx}>
      <Section className="w-full">
        <Text colors={{ background: {color, weight}, text: { color: 'slate' } }}>
          {value}
       </Text> 
      </Section>
    </Unstyled>
  })
}



