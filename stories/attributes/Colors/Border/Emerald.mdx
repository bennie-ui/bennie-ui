import { Unstyled } from "@storybook/blocks";
import { Section } from '@phoenix-ui/section'
import { Text } from '@phoenix-ui/text'
import { variants } from '@phoenix-ui/baseline/tailwind/colors/border/sm'

# Demo of supported border values

## Example
```
<Section colors={{
    border: { color: 'emerald', weight: '400' }
  }}>
  Content
</Section>
```
## Types
[See Margin Values](https://github.com/bolanosdev/phoenix-ui/blob/main/libraries/types/sizing/width.types.ts)


# Compiled tests
{
  variants.base.emerald.map((value, idx) => {
    const border_values = value.split('-')
    const border_color = border_values[1]
    const border_weight = border_values[2]

    return <Unstyled key={idx}>
     <Section colors={{
          border: { color: border_color, weight: border_weight }
        }}>
        {value}
     </Section> 
    </Unstyled>
  })
}


