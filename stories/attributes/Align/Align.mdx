import { Unstyled } from "@storybook/blocks";
import { Section } from '../../../components/primitives/section'
import { Text } from '../../../components/primitives/text'
import { variants } from '../../../libraries/baseline/tailwind/attributes/align'

# Demo of supported text "align"" values

## Example
```
<Section align={{ value: 'left' }}>Content</Section>
```
## Types
[See Margin Values](https://github.com/bolanosdev/phoenix-ui/blob/main/libraries/types/sizing/width.types.ts)


# Compiled tests
{
  variants.base.map((value, idx) => {
    const align_value =  value.split('-')[1]

    return <Unstyled key={idx}>
     <Section className="border" width={{value: 'w-full'}} align={align_value}>
        {align_value}
     </Section> 
    </Unstyled>
  })
}


